# Economy Config Schema Reference

(Placeholder — to be filled in as part of [Z1-1.5] "Document schema + add a
new economy" guide.)

Will document: every field, type, required/default, and a worked walkthrough
of adding `economies/my.yaml` (Malaysia) from scratch to a passing
`load_economy("malaysia")` call — no code changes.
