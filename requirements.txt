# Core
pyyaml
pydantic>=2
python-dotenv

# Crawling (Zone 1) — [Z1-3]
crawl4ai
playwright
tldextract
openpyxl

# HTTP probing (Zone 1) — [Z1-2]
httpx

# Document processing (Zone 2) — [Z2-1]
pdfplumber
beautifulsoup4
lxml
python-docx   # .docx extraction branch (ADR-044) — Word-only portals + AU Word downloads
pytesseract
paddleocr
pymupdf
opencv-python
Pillow
chardet
fpdf2

# Translation — [Z2-2], [Z1-2-ST5]
deepl
googletrans>=4.0.0rc1

# Retrieval / RAG (Zone 2) — [Z2-3]
sentence-transformers
rank-bm25
faiss-cpu

# LLM clients — [Z2-4] (7-tier cascade: anthropic -> openai -> deepseek -> groq -> qwen -> ollama)
anthropic
openai
groq
ollama
# deepseek: uses openai package with base_url (no extra package needed)
# qwen/dashscope: uses openai package with base_url (no extra package needed)

# Output / utils — [Z2-6]
pandas
waybackpy

# Testing
pytest
pytest-asyncio
pytest-mock
respx
argostranslate>=1.11.0  # offline neural MT — primary translator (en<->ms etc.)

# ── UI ──
gradio>=5  # frontend (app.py)

# ── Auth / Multi-Tenant (phase-2) ──
google-auth>=2.30  # Google id_token verification (Wave 1D)
cryptography>=42.0  # Fernet encryption for per-user API keys (Wave 1A)
